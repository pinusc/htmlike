// Generated by CoffeeScript 1.9.3
(function() {
  window.distance = function(coord1, coord2) {
    var difx, dify, dist, x1, x2, y1, y2;
    if (coord1.isEntity) {
      coord1 = [coord1.posx, coord1.posy];
    }
    if (coord2.isEntity) {
      coord2 = [coord2.posx, coord2.posy];
    }
    x1 = coord1[0];
    y1 = coord1[1];
    x2 = coord2[0];
    y2 = coord2[1];
    difx = x1 - x2;
    dify = y1 - y2;
    dist = Math.sqrt(difx * difx + dify * dify);
    return dist;
  };

  window.direction = function(x1, y1, x2, y2) {
    var xdiff, ydiff;
    xdiff = Math.abs(x1 - x2);
    ydiff = Math.abs(y1 - y2);
    if (xdiff > ydiff) {
      if (x1 > x2) {
        return "right";
      } else {
        return "left";
      }
    } else {
      if (y1 > y2) {
        return "down";
      } else {
        return "up";
      }
    }
  };

  window.roll = function(number, faces) {
    var i, j, ref, total;
    total = 0;
    for (i = j = 0, ref = number; 0 <= ref ? j < ref : j > ref; i = 0 <= ref ? ++j : --j) {
      total += Math.floor(Math.random() * (faces - 1 + 1)) + 1;
    }
    return total;
  };

  window.r2d6 = function() {
    return roll(2, 6);
  };

}).call(this);

//# sourceMappingURL=util.js.map
